services:
  - type: web
    name: har-ml-service
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn ml_service:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.10.0"
